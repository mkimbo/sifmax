import { Block } from 'payload'

const Appointments: Block = {
  slug: 'appointmentsPage',
  interfaceName: 'Appointment',
  labels: {
    singular: 'Appointment',
    plural: 'Appointments',
  },
  fields: [],
}

export default Appointments
